{
  "version": 2,
  "builds": [
    {
      "src": "packages/nextjs/package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "apt-get update && apt-get install -y python3-distutils && yarn install"
      }
    }
  ]
}
